/** Keys reales de POP_PERMS. OR por verbo. */

export const SERVICE_CATEGORY_READ = [
  "services:read",
  "service_charges:read",
] as const

export const SERVICE_CATEGORY_CREATE = ["services:create"] as const

export const SERVICE_CATEGORY_UPDATE = ["services:update"] as const

export const SERVICE_CATEGORY_DELETE = ["services:delete"] as const
