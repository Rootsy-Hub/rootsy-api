/** Keys reales de POP_PERMS. OR por verbo. */

export const SERVICE_READ = ["services:read"] as const

export const SERVICE_CREATE = ["services:create"] as const

export const SERVICE_UPDATE = ["services:update"] as const

export const SERVICE_DELETE = ["services:delete"] as const
